<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Affiliate System</title>
  <link rel="stylesheet" href="/css/style.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    nav.fixed-nav {
      width: 100%;
      background: #fff;
      border-bottom: 1px solid #eee;
      padding: 0;
      box-shadow: 0 2px 8px rgba(0,0,0,0.03);
      position: fixed;
      top: 0;
      left: 0;
      z-index: 1000;
    }
    .nav-inner {
      max-width: 480px;
      margin: 0 auto;
      display: flex;
      gap: 8px;
      justify-content: center;
      align-items: center;
      padding: 0 8px;
      height: 48px;
    }
    .nav-link {
      color: #007bff;
      text-decoration: none;
      font-weight: 500;
      padding: 7px 16px;
      border-radius: 7px;
      transition: background 0.18s, color 0.18s;
      font-size: 1em;
      display: inline-block;
    }
    .nav-link.active,
    .nav-link:hover {
      background: linear-gradient(90deg, #e6f0ff 60%, #e0f7fa 100%);
      color: #0056b3;
    }
    @media (max-width: 600px) {
      .nav-inner {
        max-width: 100vw;
        gap: 4px;
        font-size: 0.97em;
        padding: 0 2px;
      }
      .nav-link {
        padding: 7px 7px;
        font-size: 0.97em;
      }
    }
    body {
      padding-top: 54px;
    }
  </style>
</head>
<body>
<nav class="fixed-nav">
  <div class="nav-inner">
    <a href="/" class="nav-link<%= (typeof page !== 'undefined' && page === 'home') ? ' active' : '' %>">Trang chủ</a>
    <a href="/api/ping" class="nav-link">API Ping</a>
    <a href="/admin/login" class="nav-link">Đăng nhập Admin</a>
    <a href="/admin/shops/create" class="nav-link<%= (typeof page !== 'undefined' && page === 'createShop') ? ' active' : '' %>">Tạo Shop</a>
    <a href="/shop/testshop/login" class="nav-link">Đăng nhập Shop</a>
  </div>
</nav>
